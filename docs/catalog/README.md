# docs/catalog/

Per-product writeups promoted from `store/docs/catalog/` (61 sheets).

These are **operator reference** — not loaded by the live shop. Catalog truth remains `store/square_products_latest.json`.
